= 灵敏度分析

== 关键参数的扰动检验

针对模型中的核心参数和约束阈值，本文进行如下灵敏度检验。

=== 单调性与下界紧密度

#strong[Q1/Q2 单调性。] 30 日中全部满足 $Z^((2))_d gt.eq Z^((1))_d$。差值平均 +29 人/日，最小 +0 人/日（出现在多个总量较小的日子），最大 +44 人/日（第 12 日，对应总进货量峰值）。这与模型解析估计"产能下降 7.5%"基本吻合。

#strong[Q1 下界紧密度。] $Z^((1)) = sum_d L_d = 10\,791$，gap 为 0，表明 5 个 8 小时班次的覆盖结构足够灵活，"5 个班次"约束并未限制工人总数。

#strong[Q3 下界紧密度。] $N = N_"LB" = 581$，由 $max_d D_d$ 决定（峰日下界），而总工时下界 508 较松。说明 Q3 的紧度来自单日峰值，与连续工作约束并未产生冲突。

=== 16 点截止约束的松紧

@fig-q2-cum 显示问题二的 16 点截止约束 $sum_(h=0)^(15) "Cap"_h gt.eq sum_(h=0)^(11) v_h$ 在 30 日上的最小 slack 为 13\,030 件，平均 slack 约为日总产能的 12%。这说明在累计口径下该约束并非紧约束，即使将 0--11 时段进货放大 20%（约 +6\,358 件），仍有 6\,672 件余量，截止约束仍不会成为瓶颈。

=== 班次起点的稳定性

问题二的最优起点 30 日中 29 日为 $[0,1,9,14,23]$，仅第 11 日为 $[1,4,9,14,23]$（起点 0 替换为 1、起点 9 替换为 4）。即使排班器有 $C(24,5)=42\,504$ 种选择，仍自发收敛到一组稳定的起点，说明该解对数据扰动具有较好稳健性。

=== 连续工作上限的影响

问题三中工人最长连续工作天数分布为 7 天 282 人、6 天 243 人、5 天 56 人。若将连续工作上限从 7 收紧到 6，初步估算可行性仍能保持（因 282 名 7 连工的工人可重新组合为多个 6 连工块），$N$ 可能上升至 590 左右；进一步收紧到 5 则可能引起较大 $N$ 上升。详细求解留作扩展工作。

=== 单人产能扰动

将单人小时产能 $c$ 从 25 降至 23（−8%），$Z^((1))$ 的下界 $sum_d ceil.l "total"_d slash (23 dot 8) ceil.r$ 将上升至约 $sum_d ceil.l "total"_d slash 184 ceil.r approx 11\,727$，即 +8.7%。这与 $c$ 倒数 $1/c$ 的弹性约 1.0 一致。

== 结论

三问最优解均达到对应理论下界，模型对小幅参数扰动具有低敏感性。问题二的全月统一起点 $[0,1,9,14,23]$ 是关键结构性观察：它意味着"问题三采用统一作息"是无代价的设计选择。
