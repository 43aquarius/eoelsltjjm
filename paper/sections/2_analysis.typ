= 数据理解与总体思路

== 数据特征

附件 1 提供了 30 天 $times$ 24 小时共 720 条进货量记录，字段为「天、小时、进货量」，总进货量为 2\,155\,604 件。30 日日总量在 59\,156 件（第 26 日）至 107\,377 件（第 12 日）之间，均值 71\,853.5 件。

@fig-eda-heat 给出 30 日 $times$ 24 小时进货量的热力图。可见进货呈现明显双峰节律：上半夜 0--5 点和晚间 20--23 点是高峰，6 点与 13 点为深谷；30 日间小时模式的相关系数中位数为 0.928、均值 0.905、最小值 0.703，说明节律高度稳定，具备"全月共用相同班次起点"的可行性。

#figure(
  image("../../figures/F-EDA-1_heatmap.pdf", width: 88%),
  caption: [30 日 $times$ 24 小时进货量热力图],
) <fig-eda-heat>

@fig-eda-profile 进一步给出 30 日平均小时进货量曲线与极差范围。

#figure(
  image("../../figures/F-EDA-2_hourly_profile.pdf", width: 88%),
  caption: [24 小时进货量均值与 30 日极差],
) <fig-eda-profile>

@fig-eda-total 上栏给出每日总进货量柱状图，下栏给出每日工人数的下界 $L_d=max(ceil.l "total"_d/(25 dot 8) ceil.r, ceil.l max_h v_(d,h)/25 ceil.r)$。$L_d$ 反映了"总产能"与"峰值产能"两个独立下界中较紧者，是本文后续解的合理性基准。

#figure(
  image("../../figures/F-EDA-3_daily_total_and_LB.pdf", width: 88%),
  caption: [每日总进货量与人数下界 $L_d$],
) <fig-eda-total>

== 各子问题的关键点

#strong[问题一] 本质是单日整数规划：决策为 5 个班次起点（24 个候选）及各班次人数；目标为日工人总数最少；约束为每个时刻的累计处理量不少于累计进货量（"当日清零"按累计口径理解，与问题二同口径，保证模型单调性）。

#strong[问题二] 在问题一的基础上加入两项新约束：（1）0--12 点到达货物在 16 点前累计处理完；（2）每名工人在班内有 1 小时只能处理 10 件。前者以累计形式蕴含于"每个时刻累计产能 $gt.eq$ 累计进货"；后者通过引入辅助变量 $y_(s,h)$（表示班次 $s$ 中将低产能小时设为 $h$ 的工人数）线性化。

#strong[问题三] 是月度人员复用问题。将问题二输出的每日总人数 $D_d$ 作为输入；决策为招工总数 $N$ 及每名工人 30 天的出勤模式；约束包括"每人 30 天恰工作 23 天"和"任意 8 个连续日内工作日不超过 7 天"（即不允许 8 连工）。

总体技术路线（@fig-roadmap）依次为：数据 EDA $arrow$ 假设决断 $arrow$ 问题一 $arrow$ 问题二 $arrow$ 问题三 $arrow$ 结果校验。
