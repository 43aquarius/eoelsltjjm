= 模型假设

为简化问题并保证后续模型可解，本文做出以下假设：

+ #strong[H1（整点起点）] 每个 8 小时班次的起点为整点，即起点 $s in {0, 1, ..., 23}$。该假设与实际作息管理一致，并使候选起点为有限离散集。

+ #strong[H2（班次跨午夜循环）] 班次 $s$ 覆盖的 8 个小时为 $"Shift"(s) = {(s+i) mod 24 : i=0,...,7}$。允许班次跨越午夜，与夜班作业现实相符。

+ #strong[H3（当日清零按累计口径理解）] "当天货物当天处理完"被解释为：每个时刻 $H$ 的累计处理量不少于累计进货量；当 $H=23$ 时即"当日总处理量 $gt.eq$ 总进货量"。这一口径同时保证 Q2 的约束严于 Q1，构成单调链。

+ #strong[H4（16 点截止按累计口径理解）] 问题二中"0--12 点进货须在 16 点前处理完"对应 $sum_(h=0)^(15) "Cap"_h gt.eq sum_(h=0)^(11) v_h$（容量口径），允许在该窗口内排队等待，但 16 点末须全部清零。

+ #strong[H5（低产能小时由排班者指派）] 每个班次的"1 小时低产能"由排班者从该班次 8 个小时中选择，且同一班次内可将不同工人分配到不同的低产能小时。通过引入辅助变量 $y_(s,h)$ 线性建模。

+ #strong[H6（产能为均匀速率）] 单人单小时常规处理能力为 25 件、低产能小时为 10 件，均匀速率假设，不区分小时内更细的时段。

+ #strong[H7（货物到达即可处理）] 当小时到达的货物可以在同小时或后续小时内被处理。

+ #strong[H8（连续工作约束按 8 日窗口理解）] 问题三的"连续工作不超过 7 天"理解为：任意 8 个连续日内工作天数不超过 7 天，即不允许出现 8 个连续工作日（题面字面"7 天内 $lt.eq$ 7 天"在每日为 0/1 时为恒成立，需修正为本式以承载实际意义）。

+ #strong[H9（工人无差异）] 同一岗位的工人技能与产能完全相同，可任意互换；问题三中跨日复用合法。

+ #strong[H10（小时进货量准确）] 附件 1 给出的逐小时进货量准确无误差，不考虑预测不确定性。
