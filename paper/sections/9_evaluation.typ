= 模型评价与推广

== 模型优点

+ #strong[理论严谨、达到下界。] 三问的最优解全部等于对应理论下界（$Z^((1))=10\,791=sum_d L_d$，$Z^((2))=11\,668$ 与"产能下降 7.5%"解析估计一致，$N=581=N_"LB"$），数学保证最优性。

+ #strong[算法分层、求解高效。] 针对每个子问题选择最匹配的算法范式：问题一用"枚举起点 + 微型 LP"避免 CBC 对二元 $delta$ 的对称性退化；问题二将班内低产能小时 $y$ 放宽为连续变量，借助 $sum_h y=xi$（$xi$ 整数）保证总人数的整数性；问题三采用列式生成思想，将 $N times 30 approx 1.8 times 10^4$ 个原始二元变量降为 767\,544 个模式，再通过 LP + MILP 两步求解。三问合计求解约 16 分钟，可在普通笔记本上复现。

+ #strong[模型层次清晰、单调可解释。] Q1 $arrow$ Q2 $arrow$ Q3 三层约束严格递增，最优值单调上升；问题二的全月最优起点 $[0,1,9,14,23]$ 同时为问题三人员复用提供天然作息基础。

+ #strong[结果鲁棒。] 灵敏度分析显示对单人产能、班次起点、连续工作上限等参数的扰动响应平缓，无突变。

== 模型缺点

+ #strong[累计口径与逐小时口径的等价性依赖假设。] 本文采用累计可行性约束承载"当日清零"的语义，允许小时层面的暂时积压；若现实中分拣中心存在仓储面积、机械上限等"瞬时排队上限"约束，则需进一步加入 $W_(d,h) lt.eq W_max$ 类约束。

+ #strong[低产能小时为连续松弛。] 低产能小时分配 $y_(d,s,h)$ 实施时为非负连续，最终回到 23 人级别的"取整排班单"还需排班员二次细调。

+ #strong[工人无差异假设。] 实际中工人体力、熟练度、合同类型可能不同；本文未区分熟练工与新工。

+ #strong[需求确定性假设。] 进货量被视为确定值，未刻画日间或小时级随机波动；面向真实运营时可引入随机规划或场景树。

== 模型推广

#strong[(1) 餐饮、客服、医院等同类问题。] 模型本质是"周期性时变需求 + 固定班次时长 + 总人数最少"的整数规划，可直接推广至：餐饮服务（按小时客流安排服务员）、呼叫中心（按时段安排接线员）、医院门诊（按时段安排医生与护士）等场景。仅需重新建模具体的产能率 $c$、班长 $L$、班次数 $K$ 与时段需求 $v$。

#strong[(2) 多技能、多工种扩展。] 若工人分为多技能等级，可将 $xi_(d,s)$ 扩展为 $xi_(d,s,r)$（$r$ 为技能等级）；引入技能—需求转化系数矩阵即可保持模型形式不变。

#strong[(3) 月度复用问题的通用列式生成法。] 问题三的"模式 + LP + MILP 列精化"方法可推广到任何"周期内每人工作天数固定 + 连续工作上限 + 每日需求"的人员调度问题，适用于护士排班、保安轮岗、网约车司机派单等领域。
